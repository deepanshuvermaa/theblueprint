# Google Maps API Key
# Get your API key from: https://console.cloud.google.com/google/maps-apis
# Required for:
# - GPS Run Tracker with map visualization
# - Route heatmap (Strava-style frequency map)
# - Real-time location tracking on map
#
# Without this key, the tracker will still work but show a placeholder instead of the map.
# Pricing: Free tier includes $200/month credit (covers ~28,000 map loads)
# Docs: https://developers.google.com/maps/documentation/javascript/get-api-key
VITE_GOOGLE_MAPS_API_KEY=your_api_key_here
